import type { Module } from '../types';
export const module9: Module = { id: 'mod-9', title: 'Iterators, Generators & Symbols', description: 'The iteration protocol, custom iterables, generator functions, yield, Symbol.iterator, and async iterators.',
  topics: [
    { id: 'mod9-t1', title: 'Iterators & Symbol.iterator', explanation: `## The Iteration Protocol\n\nAn object is **iterable** if it has a \`[Symbol.iterator]()\` method that returns an **iterator** (an object with a \`next()\` method).\n\n\`\`\`javascript\n// Built-in iterables: Array, String, Map, Set\nconst arr = [1, 2, 3];\nconst iter = arr[Symbol.iterator]();\n\niter.next(); // { value: 1, done: false }\niter.next(); // { value: 2, done: false }\niter.next(); // { value: 3, done: false }\niter.next(); // { value: undefined, done: true }\n\`\`\`\n\n### Custom Iterable\n\n\`\`\`javascript\nconst range = {\n  from: 1,\n  to: 5,\n  [Symbol.iterator]() {\n    let current = this.from;\n    const last = this.to;\n    return {\n      next() {\n        return current <= last\n          ? { value: current++, done: false }\n          : { done: true };\n      },\n    };\n  },\n};\n\nfor (const num of range) {\n  console.log(num); // 1, 2, 3, 4, 5\n}\n\nconsole.log([...range]); // [1, 2, 3, 4, 5]\n\`\`\`\n\n### What Uses Iterables?\n\n- \`for...of\`\n- Spread: \`[...iterable]\`\n- Destructuring: \`const [a, b] = iterable\`\n- \`Array.from(iterable)\`\n- \`new Map(iterable)\`, \`new Set(iterable)\`\n- \`Promise.all(iterable)\``, task: { description: 'Create a custom iterable class called InfiniteCounter that yields numbers starting from a given value. Use Symbol.iterator.', starterCode: `class InfiniteCounter {\n  constructor(start = 0) {\n    // TODO\n  }\n  // TODO: implement Symbol.iterator\n}\n\n// Usage:\n// const counter = new InfiniteCounter(10);\n// for (const n of counter) {\n//   if (n > 15) break;\n//   console.log(n); // 10, 11, 12, 13, 14, 15\n// }`, solution: `class InfiniteCounter {\n  constructor(start = 0) {\n    this.start = start;\n  }\n\n  [Symbol.iterator]() {\n    let current = this.start;\n    return {\n      next() {\n        return { value: current++, done: false };\n      },\n    };\n  }\n}\n\nconst counter = new InfiniteCounter(10);\nfor (const n of counter) {\n  if (n > 15) break;\n  console.log(n); // 10, 11, 12, 13, 14, 15\n}\n\n// Also works with destructuring:\nconst [a, b, c] = new InfiniteCounter(100);\nconsole.log(a, b, c); // 100, 101, 102`, hints: ['Implement [Symbol.iterator]() returning an object with next()', 'next() returns { value, done }', 'For infinite iterators, done is always false — use break to stop'] } },
    { id: 'mod9-t2', title: 'Generator Functions', explanation: `## Generators\n\nGenerators are functions that can be paused and resumed with \`yield\`.\n\n\`\`\`javascript\nfunction* fibonacci() {\n  let [a, b] = [0, 1];\n  while (true) {\n    yield a;\n    [a, b] = [b, a + b];\n  }\n}\n\nconst fib = fibonacci();\nconsole.log(fib.next().value); // 0\nconsole.log(fib.next().value); // 1\nconsole.log(fib.next().value); // 1\nconsole.log(fib.next().value); // 2\nconsole.log(fib.next().value); // 3\n\`\`\`\n\n### Generators Are Iterables\n\n\`\`\`javascript\nfunction* range(start, end) {\n  for (let i = start; i <= end; i++) {\n    yield i;\n  }\n}\n\nconsole.log([...range(1, 5)]); // [1, 2, 3, 4, 5]\n\nfor (const n of range(10, 13)) {\n  console.log(n); // 10, 11, 12, 13\n}\n\`\`\`\n\n### Two-Way Communication\n\n\`\`\`javascript\nfunction* dialog() {\n  const name = yield "What is your name?";\n  const age = yield \`Hello \${name}! How old are you?\`;\n  return \`\${name} is \${age} years old.\`;\n}\n\nconst d = dialog();\nconsole.log(d.next().value);         // "What is your name?"\nconsole.log(d.next("Alice").value);  // "Hello Alice! How old are you?"\nconsole.log(d.next(30).value);       // "Alice is 30 years old."\n\`\`\`\n\n### yield* Delegation\n\n\`\`\`javascript\nfunction* inner() {\n  yield 3;\n  yield 4;\n}\n\nfunction* outer() {\n  yield 1;\n  yield 2;\n  yield* inner(); // Delegate to inner\n  yield 5;\n}\n\nconsole.log([...outer()]); // [1, 2, 3, 4, 5]\n\`\`\`\n\n### Async Generators\n\n\`\`\`javascript\nasync function* fetchPages(url) {\n  let page = 1;\n  while (true) {\n    const res = await fetch(\`\${url}?page=\${page}\`);\n    const data = await res.json();\n    if (data.length === 0) return;\n    yield data;\n    page++;\n  }\n}\n\nfor await (const page of fetchPages("/api/items")) {\n  console.log(page);\n}\n\`\`\``, task: { description: 'Create a generator that implements a simple state machine for a traffic light: green → yellow → red → green (repeating).', starterCode: `function* trafficLight() {\n  // TODO: Yield "green", "yellow", "red" in a loop\n}\n\n// Usage:\n// const light = trafficLight();\n// light.next().value → "green"\n// light.next().value → "yellow"\n// light.next().value → "red"\n// light.next().value → "green" (repeats)`, solution: `function* trafficLight() {\n  const states = ["green", "yellow", "red"];\n  let index = 0;\n  while (true) {\n    yield states[index];\n    index = (index + 1) % states.length;\n  }\n}\n\nconst light = trafficLight();\nconsole.log(light.next().value); // "green"\nconsole.log(light.next().value); // "yellow"\nconsole.log(light.next().value); // "red"\nconsole.log(light.next().value); // "green"\nconsole.log(light.next().value); // "yellow"\n\n// Can also iterate:\nlet count = 0;\nfor (const state of trafficLight()) {\n  console.log(state);\n  if (++count >= 9) break; // 3 full cycles\n}`, hints: ['Use while(true) to repeat the cycle indefinitely', 'yield pauses and returns the current state', 'Use modulo (%) to cycle through the array'] } },
  ],
  test: [
    { id: 'mod9-q1', question: 'What method must an iterable implement?', options: ['next()', 'iterate()', '[Symbol.iterator]()', '[Symbol.for]()'], correctAnswer: 2, explanation: 'The iterable protocol requires a [Symbol.iterator]() method that returns an iterator object (which itself must have a next() method). Note the distinction: the iterable has [Symbol.iterator](), the iterator has next(). Built-in iterables include Array, String, Map, Set, TypedArray, arguments, and NodeList.' },
    { id: 'mod9-q2', question: 'What does yield do in a generator?', options: ['Stops the function permanently', 'Pauses execution and returns a value', 'Creates a new thread', 'Throws an error'], correctAnswer: 1, explanation: 'yield pauses the generator function and returns { value, done: false } to the caller. The function\'s state (local variables, position) is preserved. When next() is called again, execution resumes from right after the yield. This "lazy evaluation" is powerful for infinite sequences, paginated data, and cooperative multitasking — values are produced on demand rather than all at once.' },
    { id: 'mod9-q3', question: 'What does yield* do?', options: ['Yields all values', 'Delegates to another iterable/generator', 'Yields undefined', 'Creates a sub-generator'], correctAnswer: 1, explanation: 'yield* delegates iteration to another iterable or generator, yielding each of its values one by one. It\'s syntactic sugar for: for (const val of otherIterable) { yield val; }. This is essential for composing generators — breaking complex sequences into reusable sub-generators. The return value of yield* is the return value of the delegated generator.' },
    { id: 'mod9-q4', question: 'How do you pass data INTO a generator?', options: ['As constructor argument', 'Via generator.next(value)', 'Through global state', 'With yield.set()'], correctAnswer: 1, explanation: 'Calling generator.next(value) sends value into the generator, where it becomes the return value of the currently paused yield expression. NOTE: the FIRST next() call starts the generator up to the first yield — any value passed to the first next() is discarded. Two-way communication: yield sends data OUT, next(val) pushes data IN. This enables conversational patterns like dialog systems.' },
    { id: 'mod9-q5', question: 'What is an async generator?', options: ['A faster generator', 'A generator that can use await and is consumed with for-await-of', 'A generator that runs in parallel', 'A deprecated feature'], correctAnswer: 1, explanation: 'Async generators (async function*) combine generators with async/await: they can yield values asynchronously using await inside the function body. Consumed with for await...of. Real-world use cases: paginated API calls (fetch page, yield results, repeat), streaming data processing, and reading files line by line. Each yield produces a Promise that the consumer awaits.' },
  ],
};
