import type { Module } from '../types';
export const module17: Module = { id: 'mod-17', title: 'Decorators & Metadata', description: 'TC39 decorators, legacy decorators, reflect-metadata, and decorator patterns for classes, methods, and properties.',
  topics: [
    { id: 'mod17-t1', title: 'Decorators (TC39 & Legacy)', explanation: `## Decorators\n\nDecorators add metadata or modify classes, methods, properties, and parameters.\n\n### TC39 Stage 3 Decorators (TS 5.0+)\n\n\`\`\`typescript\n// Method decorator\nfunction log(\n  target: any,\n  context: ClassMethodDecoratorContext\n) {\n  const methodName = String(context.name);\n  return function (this: any, ...args: any[]) {\n    console.log(\`→ \${methodName}(\${args.join(", ")})\`);\n    const result = target.call(this, ...args);\n    console.log(\`← \${methodName} = \${result}\`);\n    return result;\n  };\n}\n\nclass Calculator {\n  @log\n  add(a: number, b: number): number {\n    return a + b;\n  }\n}\n\nnew Calculator().add(2, 3);\n// → add(2, 3)\n// ← add = 5\n\`\`\`\n\n### Class Decorator\n\n\`\`\`typescript\nfunction sealed(\n  target: Function,\n  context: ClassDecoratorContext\n) {\n  Object.seal(target);\n  Object.seal(target.prototype);\n}\n\n@sealed\nclass API {\n  version = \"1.0\";\n}\n\`\`\`\n\n### Legacy Decorators (experimentalDecorators)\n\n\`\`\`typescript\n// Still widely used in Angular, NestJS, TypeORM\nfunction Injectable() {\n  return function <T extends new (...args: any[]) => any>(constructor: T) {\n    // Register in DI container\n    return constructor;\n  };\n}\n\n@Injectable()\nclass UserService {\n  getUser(id: number) { /* ... */ }\n}\n\n// Property decorator\nfunction Column(options?: { type: string }) {\n  return function (target: any, propertyKey: string) {\n    Reflect.defineMetadata(\"column\", options, target, propertyKey);\n  };\n}\n\nclass User {\n  @Column({ type: \"varchar\" })\n  name!: string;\n\n  @Column({ type: \"int\" })\n  age!: number;\n}\n\`\`\`\n\n### Decorator Factories\n\n\`\`\`typescript\nfunction minLength(min: number) {\n  return function (\n    target: any,\n    context: ClassFieldDecoratorContext\n  ) {\n    return function (initialValue: string) {\n      if (initialValue.length < min) {\n        throw new Error(\`\${String(context.name)} must be at least \${min} chars\`);\n      }\n      return initialValue;\n    };\n  };\n}\n\nclass Form {\n  @minLength(3)\n  username = \"Bob\";\n}\n\`\`\``, task: { description: 'Create a method decorator that memoizes (caches) the results of pure function calls.', starterCode: `// TODO: Create a @memoize decorator\n// Should cache results based on arguments\n\nclass MathUtils {\n  // @memoize\n  fibonacci(n: number): number {\n    if (n <= 1) return n;\n    return this.fibonacci(n - 1) + this.fibonacci(n - 2);\n  }\n}`, solution: `function memoize(\n  target: any,\n  context: ClassMethodDecoratorContext\n) {\n  const cache = new Map<string, any>();\n\n  return function (this: any, ...args: any[]) {\n    const key = JSON.stringify(args);\n    if (cache.has(key)) {\n      return cache.get(key);\n    }\n    const result = target.call(this, ...args);\n    cache.set(key, result);\n    return result;\n  };\n}\n\nclass MathUtils {\n  @memoize\n  fibonacci(n: number): number {\n    if (n <= 1) return n;\n    return this.fibonacci(n - 1) + this.fibonacci(n - 2);\n  }\n}\n\nconst math = new MathUtils();\nconsole.time("first");\nmath.fibonacci(40);  // Computes all values\nconsole.timeEnd("first");\n\nconsole.time("cached");\nmath.fibonacci(40);  // Instant from cache\nconsole.timeEnd("cached");`, hints: ['Use a Map to cache results keyed by JSON.stringify(args)', 'Return a replacement function that checks the cache first', 'Call target.call(this, ...args) for cache misses'] } },
  ],
  test: [
    { id: 'mod17-q1', question: 'What is a decorator?', options: ['A design pattern for UI', 'A function that modifies or adds metadata to classes/members', 'A CSS framework', 'A testing utility'], correctAnswer: 1, explanation: 'Decorators are functions that receive a target (class, method, property) and can modify it or attach metadata.' },
    { id: 'mod17-q2', question: 'What is a decorator factory?', options: ['A class that creates decorators', 'A function that returns a decorator (allows configuration)', 'A built-in TypeScript feature', 'A compiler plugin'], correctAnswer: 1, explanation: 'A decorator factory is a function that accepts parameters and returns the actual decorator function, enabling configuration.' },
    { id: 'mod17-q3', question: 'Which TypeScript config enables legacy decorators?', options: ['strictDecorators', 'experimentalDecorators', 'enableDecorators', 'decorators'], correctAnswer: 1, explanation: 'The experimentalDecorators compiler option enables the legacy decorator syntax used by Angular, NestJS, etc.' },
    { id: 'mod17-q4', question: 'In what order do decorators execute?', options: ['Top to bottom', 'Bottom to top (closest to target first)', 'Alphabetically', 'Random'], correctAnswer: 1, explanation: 'Multiple decorators compose bottom-up: the decorator closest to the declaration runs first.' },
    { id: 'mod17-q5', question: 'TC39 Stage 3 decorators are available in TypeScript starting from:', options: ['TypeScript 3.0', 'TypeScript 4.0', 'TypeScript 5.0', 'Not yet available'], correctAnswer: 2, explanation: 'TypeScript 5.0 introduced support for TC39 Stage 3 decorators alongside the legacy experimentalDecorators.' },
  ],
};
