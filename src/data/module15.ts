import type { Module } from '../types';
export const module15: Module = { id: 'mod-15', title: 'Generics Deep Dive', description: 'Generic functions, classes, constraints, defaults, variance, and advanced patterns.',
  topics: [
    { id: 'mod15-t1', title: 'Generic Functions & Constraints', explanation: `## Generics\n\nGenerics let you write type-safe reusable code without losing type information.\n\n### Basic Generics\n\n\`\`\`typescript\nfunction identity<T>(value: T): T {\n  return value;\n}\n\nconst s = identity("hello"); // string\nconst n = identity(42);      // number\n\n// Generic with arrays\nfunction first<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\n\nconst f = first([1, 2, 3]); // number | undefined\n\`\`\`\n\n### Constraints\n\n\`\`\`typescript\n// T must have a length property\nfunction longest<T extends { length: number }>(a: T, b: T): T {\n  return a.length >= b.length ? a : b;\n}\n\nlongest("abc", "de");    // ✓ string has length\nlongest([1, 2], [3]);    // ✓ array has length\n// longest(10, 20);      // ✗ number has no length\n\n// keyof constraint\nfunction getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {\n  return obj[key];\n}\n\nconst user = { name: "Alice", age: 30 };\ngetProperty(user, "name"); // string\ngetProperty(user, "age");  // number\n// getProperty(user, "xxx"); // Error: "xxx" is not assignable\n\`\`\`\n\n### Multiple Type Parameters\n\n\`\`\`typescript\nfunction zip<A, B>(a: A[], b: B[]): [A, B][] {\n  const length = Math.min(a.length, b.length);\n  const result: [A, B][] = [];\n  for (let i = 0; i < length; i++) {\n    result.push([a[i], b[i]]);\n  }\n  return result;\n}\n\nconst pairs = zip([1, 2, 3], ["a", "b", "c"]);\n// [number, string][]\n\`\`\`\n\n### Default Type Parameters\n\n\`\`\`typescript\ninterface ApiResponse<T = unknown> {\n  data: T;\n  error?: string;\n}\n\ntype UntypedResponse = ApiResponse;         // data: unknown\ntype UserResponse = ApiResponse<User>;      // data: User\n\`\`\``, tasks: [{ description: 'Create a generic Result<T, E> type (like Rust\'s Result) with Ok and Err variants, plus map/flatMap methods.', starterCode: `// TODO: Implement Result<T, E>\n// Ok<T> and Err<E> variants\n// .map(fn: T => U): Result<U, E>\n// .flatMap(fn: T => Result<U, E>): Result<U, E>`, solution: `type Result<T, E> = Ok<T, E> | Err<T, E>;\n\nclass Ok<T, E = never> {\n  readonly _tag = "Ok" as const;\n  constructor(readonly value: T) {}\n\n  map<U>(fn: (value: T) => U): Result<U, E> {\n    return new Ok(fn(this.value));\n  }\n\n  flatMap<U>(fn: (value: T) => Result<U, E>): Result<U, E> {\n    return fn(this.value);\n  }\n\n  isOk(): this is Ok<T, E> { return true; }\n  isErr(): this is Err<T, E> { return false; }\n}\n\nclass Err<T = never, E = unknown> {\n  readonly _tag = "Err" as const;\n  constructor(readonly error: E) {}\n\n  map<U>(_fn: (value: T) => U): Result<U, E> {\n    return this as unknown as Err<U, E>;\n  }\n\n  flatMap<U>(_fn: (value: T) => Result<U, E>): Result<U, E> {\n    return this as unknown as Err<U, E>;\n  }\n\n  isOk(): this is Ok<T, E> { return false; }\n  isErr(): this is Err<T, E> { return true; }\n}\n\nfunction ok<T>(value: T): Ok<T, never> { return new Ok(value); }\nfunction err<E>(error: E): Err<never, E> { return new Err(error); }\n\n// Usage\nconst result = ok(42)\n  .map(x => x * 2)\n  .flatMap(x => x > 50 ? ok(x) : err("too small"));\n\nif (result.isOk()) {\n  console.log(result.value); // 84\n}`, hints: ['Use discriminated union with _tag property', 'Ok.map applies the function, Err.map passes through', 'flatMap is like map but the function returns a Result'] }] },
    { id: 'mod15-t2', title: 'Generic Classes & Patterns', explanation: `## Generic Classes\n\n\`\`\`typescript\nclass Stack<T> {\n  private items: T[] = [];\n\n  push(item: T): void {\n    this.items.push(item);\n  }\n\n  pop(): T | undefined {\n    return this.items.pop();\n  }\n\n  peek(): T | undefined {\n    return this.items[this.items.length - 1];\n  }\n\n  get size(): number {\n    return this.items.length;\n  }\n}\n\nconst numStack = new Stack<number>();\nnumStack.push(1);\nnumStack.push(2);\nnumStack.pop(); // number | undefined\n\`\`\`\n\n### Generic Interfaces\n\n\`\`\`typescript\ninterface Repository<T extends { id: string | number }> {\n  findById(id: T["id"]): Promise<T | null>;\n  findAll(): Promise<T[]>;\n  save(entity: T): Promise<T>;\n  delete(id: T["id"]): Promise<boolean>;\n}\n\nclass UserRepository implements Repository<User> {\n  async findById(id: number): Promise<User | null> { /* ... */ }\n  async findAll(): Promise<User[]> { /* ... */ }\n  async save(user: User): Promise<User> { /* ... */ }\n  async delete(id: number): Promise<boolean> { /* ... */ }\n}\n\`\`\`\n\n### Builder Pattern with Generics\n\n\`\`\`typescript\nclass QueryBuilder<T> {\n  private filters: Array<(item: T) => boolean> = [];\n  private sortFn?: (a: T, b: T) => number;\n  private limitCount?: number;\n\n  where(predicate: (item: T) => boolean): this {\n    this.filters.push(predicate);\n    return this;\n  }\n\n  orderBy<K extends keyof T>(key: K, dir: "asc" | "desc" = "asc"): this {\n    this.sortFn = (a, b) => {\n      const va = a[key], vb = b[key];\n      const cmp = va < vb ? -1 : va > vb ? 1 : 0;\n      return dir === "asc" ? cmp : -cmp;\n    };\n    return this;\n  }\n\n  limit(count: number): this {\n    this.limitCount = count;\n    return this;\n  }\n\n  execute(data: T[]): T[] {\n    let result = data.filter(item => this.filters.every(f => f(item)));\n    if (this.sortFn) result.sort(this.sortFn);\n    if (this.limitCount !== undefined) result = result.slice(0, this.limitCount);\n    return result;\n  }\n}\n\n// Usage\nconst users = new QueryBuilder<User>()\n  .where(u => u.age >= 18)\n  .orderBy("name")\n  .limit(10)\n  .execute(allUsers);\n\`\`\``, tasks: [{ description: 'Create a generic TypedEventEmitter<EventMap> class where events and their payload types are defined by a type parameter.', starterCode: `// TODO: Implement TypedEventEmitter<Events>\n// Events is a Record<string, any> mapping event names to payload types`, solution: `class TypedEventEmitter<Events extends Record<string, unknown>> {\n  private listeners = new Map<keyof Events, Set<(payload: any) => void>>();\n\n  on<K extends keyof Events>(event: K, handler: (payload: Events[K]) => void): () => void {\n    if (!this.listeners.has(event)) {\n      this.listeners.set(event, new Set());\n    }\n    this.listeners.get(event)!.add(handler);\n\n    // Return unsubscribe function\n    return () => {\n      this.listeners.get(event)?.delete(handler);\n    };\n  }\n\n  emit<K extends keyof Events>(event: K, payload: Events[K]): void {\n    this.listeners.get(event)?.forEach(handler => handler(payload));\n  }\n\n  off<K extends keyof Events>(event: K, handler: (payload: Events[K]) => void): void {\n    this.listeners.get(event)?.delete(handler);\n  }\n}\n\n// Usage — fully type-safe!\ninterface AppEvents {\n  login: { userId: string; timestamp: number };\n  logout: { userId: string };\n  error: { message: string; code: number };\n}\n\nconst emitter = new TypedEventEmitter<AppEvents>();\n\nemitter.on("login", (e) => {\n  console.log(e.userId, e.timestamp); // ✓ types known!\n});\n\nemitter.emit("login", { userId: "123", timestamp: Date.now() });\n// emitter.emit("login", { wrong: true }); // ✗ type error`, hints: ['Events extends Record<string, unknown> defines the event map', 'on<K extends keyof Events> constrains the event name', 'The handler parameter type is Events[K]'] }] },
  ],
  test: [
    { id: 'mod15-q1', question: 'What does `T extends { length: number }` mean?', options: ['T inherits from an object', 'T must be a number', 'T must have a length property of type number', 'T is an array'], correctAnswer: 2, explanation: 'This is a generic constraint: T must be a type that has at least a length property of type number. Any value that structurally matches is accepted — strings ("hello".length), arrays ([1,2].length), or custom objects ({length: 5}). This is structural typing in action: TS checks shape, not naming/inheritance.' },
    { id: 'mod15-q2', question: 'What does `K extends keyof T` constrain?', options: ['K must be any string', 'K must be a key that exists on type T', 'K must be a type', 'K must be a number'], correctAnswer: 1, explanation: 'keyof T produces a union of T\'s property names as string literal types. K extends keyof T constrains K to be one of those names, so TypeScript can guarantee type-safe property access: T[K] returns the correct type. This is the foundation of typed property accessors like function get<T, K extends keyof T>(obj: T, key: K): T[K].' },
    { id: 'mod15-q3', question: 'Can generics have default type parameters?', options: ['No', 'Yes, using = syntax', 'Only in interfaces', 'Only in classes'], correctAnswer: 1, explanation: 'Generic parameters support defaults using = syntax: <T = string>. When the caller doesn\'t specify T, it defaults to string. Default parameters must come after required ones (like function defaults). This is commonly used in library APIs: React\'s useState<S = undefined>(), event emitter types, and configurable data structures.' },
    { id: 'mod15-q4', question: 'What is the purpose of the builder pattern with generics?', options: ['Performance optimization', 'Type-safe fluent API with method chaining', 'Memory management', 'Async operations'], correctAnswer: 1, explanation: 'The generic builder pattern uses generics to track which fields have been set at the TYPE level, creating a fluent API where each method returns a new type that "remembers" the state. This enables compile-time enforcement: build() can only be called when all required fields are set. Examples include query builders, request builders, and configuration APIs where you want chaining like builder.setName("x").setAge(5).build().' },
    { id: 'mod15-q5', question: 'Multiple generic parameters are separated by:', options: ['Semicolons', 'Commas', 'Pipes', 'Ampersands'], correctAnswer: 1, explanation: 'Multiple type parameters are separated by commas: <A, B, C>. Convention is to use single uppercase letters (T, U, V) or descriptive names with a T prefix (TKey, TValue). For example: function map<T, U>(arr: T[], fn: (item: T) => U): U[] uses two parameters where T is the input type and U is the output type.' },
  ],
};
